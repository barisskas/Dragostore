<main>
  <div class="container">
    <div class="row">
      <div class="col-md-5 p-4">
        <!-- Swiper -->

        <div
          style="
            --swiper-navigation-color: #fff;
            --swiper-pagination-color: #fff;
          "
          class="swiper mySwiper2"
        >
          <div class="swiper-wrapper">
            <% product.images.forEach((image, index) => { %>
            <div class="swiper-slide">
              <img
                src="/images/product/<%= categoryName%>/<%= image.url %>"
                class=""
                alt="..."
              />
            </div>
            <% }) %>
          </div>
          <div class="swiper-button-next"></div>
          <div class="swiper-button-prev"></div>
        </div>
        <div thumbsSlider="" class="swiper mySwiper">
          <div class="swiper-wrapper">
            <% product.images.forEach((image, index) => { %>
            <div class="swiper-slide">
              <img
                src="/images/product/<%= categoryName%>/<%= image.url %>"
                alt="..."
                class=""
              />
            </div>
            <% }) %>
          </div>
        </div>
        <!-- <div
          id="carouselExampleControls"
          class="carousel slide"
          data-bs-ride="carousel"
        >
          <div class="carousel-inner">
            <% product.images.forEach((image, index) => { %>
            <div class="carousel-item <%= !index && 'active' %>">
              <img
                src="/images/product/<%= categoryName%>/<%= image.url %>"
                class="d-block w-100"
                alt="..."
              />
            </div>
            <% }) %>
          </div>
          <button
            class="carousel-control-prev"
            type="button"
            data-bs-target="#carouselExampleControls"
            data-bs-slide="prev"
          >
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Previous</span>
          </button>
          <button
            class="carousel-control-next"
            type="button"
            data-bs-target="#carouselExampleControls"
            data-bs-slide="next"
          >
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="visually-hidden">Next</span>
          </button>
        </div>
        <div class="col images-product p-3">
          <div class="images-product-cover mx-auto">
            <% product.images.forEach((image, index) => { %>

            <img
              src="/images/product/<%= categoryName%>/<%= image.url %>"
              alt="..."
              class="img-thumbnail thumbnail-category m-1"
            />

            <% }) %>
          </div>
        </div> -->
      </div>
      <div class="col-md-7 p-5">
        <ul class="product-feature-list">
          <li><h2 class="product-model"><%= product.name %></h2></li>
          <li><p><%= product.features%></p></li>
          <li>
            <h2 class="product-model"><%= product.price.toFixed(2) %> TL</h2>
          </li>
          <% if(user){%>
          <li>
            <button
              id="<%= product._id %>"
              class="<%= user.baskets.includes(product._id) ? 'remove-product' : 'add-product'%> add-basket btn btn-primary raunded"
            >
              <%= user.baskets.includes(product._id) ? 'sepetten çıkar' :
              'sepete ekle' %>
            </button>
          </li>
          <% }%>
        </ul>
      </div>
    </div>
  </div>
</main>
